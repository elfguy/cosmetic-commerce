---
import BaseLayout from '../../layouts/BaseLayout.astro';
import SiteHeader from '../../components/SiteHeader.astro';
import SiteFooter from '../../components/SiteFooter.astro';
import { campaignModels, archivedModels, campaignVideos, productionGates, sceneAttempts } from '../../data/aquaLotionVideoStudio';

const statusLabel = {
  concept: '콘티 작성', casting: '캐스팅', producing: '제작 중', review: '검수', approved: '승인', published: '게시', archived: '보관',
} as const;
const attemptStatusLabel = {
  reference: '기준 이미지', pending: '생성 중', review: '검수 대기', selected: '선정', rejected: '탈락',
} as const;
const firstVideo = campaignVideos[0];
const imageAttempts = sceneAttempts.filter((attempt) => !attempt.videoSrc);
const generatedVideoAttempts = sceneAttempts.filter((attempt) => Boolean(attempt.videoSrc));
const selectedVideoCount = generatedVideoAttempts.filter((attempt) => attempt.status === 'selected').length;
const reviewVideoCount = generatedVideoAttempts.filter((attempt) => ['review', 'pending'].includes(attempt.status)).length;
const rejectedVideoCount = generatedVideoAttempts.filter((attempt) => attempt.status === 'rejected').length;
const storySketches = [
  { src: '/marketing/aqua-lotion-video-studio/storyboards-30s/01-problem-silent.webp', label: '0–7초 · 말 없는 문제 인식', desc: '모델은 입을 닫고 거울과 토너·크림 사이에서 망설임만 연기' },
  { src: '/marketing/aqua-lotion-video-studio/storyboards-30s/02-solution-product.webp', label: '7–15초 · 제품이 해결책으로 등장', desc: '나레이션 위에 실제 제품과 손의 접근을 분리해 보여줌' },
  { src: '/marketing/aqua-lotion-video-studio/storyboards-30s/03-proof-details.webp', label: '15–23초 · 제품 정보와 근거', desc: '펌프·투명 용기·8종 히알루론산을 제품 중심으로 설명' },
  { src: '/marketing/aqua-lotion-video-studio/storyboards-30s/04-finish-cta.webp', label: '23–30초 · 무언 결과와 CTA', desc: '모델은 조용히 루틴을 마치고 움직이는 제품 컷으로 종료' },
];
---

<BaseLayout title="아쿠아 로션 영상 스튜디오" description="유어스킨플러스 아쿠아 로션 광고 모델, 영상 버전, 콘티와 제작 게이트를 누적 관리하는 마케팅 영상 대시보드">
  <SiteHeader active="/marketing/aqua-lotion-video-studio/" />

  <main class="studio-page">
    <section class="hero container">
      <div class="hero-copy">
        <p class="eyebrow">Aqua lotion · moving image system</p>
        <h1>한 편을 만드는 페이지가 아니라<br /><em>계속 만드는 시스템</em></h1>
        <p>완성본을 한 번에 만들지 않고, 표정·제품 집기·펌핑·도포·마무리를 각각 생성해 비교합니다. 탈락본과 크레딧까지 남겨 같은 실패를 반복하지 않습니다.</p>
        <div class="hero-actions">
          <a href="#compare">V2 부분 비교 보기</a>
          <a class="ghost" href="#production-details" data-open-production-details>제작 상세 보기</a>
        </div>
      </div>
      <div class="hero-board" aria-label="현재 제작 현황">
        <div class="board-top"><span>PRODUCTION BOARD</span><b>2026.08.01</b></div>
        <div class="board-main">
          <strong>AQUA-UGC-001</strong>
          <h2>가볍게 한 번 더</h2>
          <p>MODEL D · V2 / <b>AI 제품 사용 테스트 중단</b></p>
        </div>
        <dl>
          <div><dt>활성 모델</dt><dd>{campaignModels.length}</dd></div>
          <div><dt>비교 후보</dt><dd>{sceneAttempts.length}</dd></div>
          <div><dt>현재 단계</dt><dd>REAL SHOT</dd></div>
        </dl>
      </div>
    </section>

    <nav class="section-nav container" aria-label="페이지 목차">
      <a href="#compare">이미지 기준</a><a href="#generated-videos">생성 영상</a><a href="#production-details" data-open-production-details>모델·콘티·게이트</a>
    </nav>

    <section class="attempt-section container" id="compare">
      <header class="section-head">
        <div><p class="eyebrow">V2 scene lab</p><h2>이미지와 영상을 분리해서 비교</h2></div>
        <p>기준 이미지와 생성 영상을 한 줄에 섞지 않습니다. 이미지는 입력 품질을, 영상은 성공·검토·실패 결과를 따로 확인합니다.</p>
      </header>
      <div class="attempt-summary">
        <div><span>이미지·키프레임</span><b>{imageAttempts.length}</b></div>
        <div><span>생성 영상</span><b>{generatedVideoAttempts.length}</b></div>
        <div><span>영상 실패</span><b>{rejectedVideoCount}</b></div>
        <div><span>V2 사용</span><b>135</b><small>credits</small></div>
      </div>

      <section class="attempt-lane image-lane" aria-labelledby="image-attempt-title">
        <header class="lane-head">
          <div><p class="lane-kicker">01 · INPUT AUTHORITY</p><h3 id="image-attempt-title">이미지·키프레임</h3></div>
          <p>좌우로 넘겨서 얼굴·의상·제품·손의 입력 품질만 비교합니다.</p>
        </header>
        <div class="attempt-rail image-rail">
          {imageAttempts.map((attempt) => (
            <article class:list={['attempt-card', 'image-attempt', attempt.status]}>
              <div class="attempt-media">
                {attempt.poster && <img src={attempt.poster} alt={`${attempt.title} 비교 이미지`} loading="eager" />}
                <span class="attempt-scene">{attempt.scene}</span>
                <b class="attempt-status">{attemptStatusLabel[attempt.status]}</b>
              </div>
              <div class="attempt-copy">
                <div class="attempt-id"><span>{attempt.id}</span><b>{attempt.creditCost}</b></div>
                <h3>{attempt.title}</h3>
                <p>{attempt.summary}</p>
                <dl><div><dt>생성 방식</dt><dd>{attempt.model}</dd></div>{attempt.duration && <div><dt>길이</dt><dd>{attempt.duration}</dd></div>}</dl>
                <details class="attempt-issues"><summary>판정 근거 <b>{attempt.issues.length}</b></summary><ul>{attempt.issues.map((issue) => <li>{issue}</li>)}</ul></details>
              </div>
            </article>
          ))}
        </div>
      </section>

      <section class="attempt-lane video-lane" id="generated-videos" aria-labelledby="video-attempt-title">
        <header class="lane-head video-lane-head">
          <div><p class="lane-kicker">02 · GENERATED MOTION</p><h3 id="video-attempt-title">생성 영상 결과</h3></div>
          <div class="video-filters" role="group" aria-label="영상 결과 필터">
            <button class="active" type="button" data-video-filter="all" data-count={generatedVideoAttempts.length}>전체 <b>{generatedVideoAttempts.length}</b></button>
            <button type="button" data-video-filter="success" data-count={selectedVideoCount}>성공 <b>{selectedVideoCount}</b></button>
            <button type="button" data-video-filter="review" data-count={reviewVideoCount}>검토 <b>{reviewVideoCount}</b></button>
            <button type="button" data-video-filter="failed" data-count={rejectedVideoCount}>실패 <b>{rejectedVideoCount}</b></button>
          </div>
        </header>
        <div class="video-outcome-strip">
          <div class="success"><span>SUCCESS</span><b>{selectedVideoCount}</b><small>사용자 승인 영상</small></div>
          <div class="review"><span>REVIEW</span><b>{reviewVideoCount}</b><small>판정 대기 영상</small></div>
          <div class="failed"><span>FAILED</span><b>{rejectedVideoCount}</b><small>마스터 사용 금지</small></div>
        </div>
        <p class="filter-empty" data-video-empty hidden>해당 상태의 생성 영상이 아직 없습니다.</p>
        <div class="attempt-rail video-attempt-rail">
          {generatedVideoAttempts.map((attempt) => {
            const videoState = attempt.status === 'selected' ? 'success' : ['review', 'pending'].includes(attempt.status) ? 'review' : 'failed';
            return (
              <article class:list={['attempt-card', 'video-attempt', attempt.status]} data-video-state={videoState}>
                <div class="attempt-media">
                  <video controls preload="metadata" poster={attempt.poster}><source src={attempt.videoSrc} type="video/mp4" /></video>
                  <span class="attempt-scene">{attempt.scene}</span>
                  <b class="attempt-status">{attemptStatusLabel[attempt.status]}</b>
                </div>
                <div class="attempt-copy">
                  <div class="attempt-id"><span>{attempt.id}</span><b>{attempt.creditCost}</b></div>
                  <h3>{attempt.title}</h3>
                  <p>{attempt.summary}</p>
                  <dl><div><dt>생성 방식</dt><dd>{attempt.model}</dd></div>{attempt.duration && <div><dt>길이</dt><dd>{attempt.duration}</dd></div>}</dl>
                  <details class="attempt-issues"><summary>판정 근거 <b>{attempt.issues.length}</b></summary><ul>{attempt.issues.map((issue) => <li>{issue}</li>)}</ul></details>
                </div>
              </article>
            );
          })}
        </div>
      </section>
    </section>

    <details class="dashboard-details" id="production-details">
      <summary class="container"><span><small>PRODUCTION ARCHIVE</small><b>모델·기존 영상·30초 콘티·제작 게이트</b></span><strong>제작 상세 펼치기</strong></summary>

    <section class="models-section container" id="models">
      <header class="section-head">
        <div><p class="eyebrow">Cast registry</p><h2>모델 정체성 보드</h2></div>
        <p>키·몸무게·직업은 실존 인물 추정이 아니라 Soul ID 일관성을 위한 가상 제작 설정입니다.</p>
      </header>

      <div class="model-grid">
        {campaignModels.map((model) => (
          <article class="model-card">
            <div class="model-visual">
              <img src={model.image} alt={`${model.name} 전신 모델 이미지`} />
              <span class="model-code">{model.code}</span>
              <span class:list={['status', model.status]}>{model.status === 'selected' ? 'SELECTED' : 'ACTIVE'}</span>
            </div>
            <div class="model-copy">
              <p class="role">{model.role}</p>
              <h3>{model.name}</h3>
              <div class="measure"><b>{model.age}세</b><span>{model.heightCm}cm</span><span>{model.weightKg}kg</span></div>
              <p class="styling">{model.styling}</p>
              <dl>
                <div><dt>설정 직업</dt><dd>{model.job}</dd></div>
                <div><dt>말투</dt><dd>{model.voice}</dd></div>
                <div><dt>성격</dt><dd>{model.personality}</dd></div>
              </dl>
              <blockquote>“{model.hook}”</blockquote>
              <div class="signal-grid">
                <div><b>강점</b>{model.strengths.map((item) => <span>{item}</span>)}</div>
                <div class="risk"><b>주의</b>{model.risks.map((item) => <span>{item}</span>)}</div>
              </div>
            </div>
          </article>
        ))}
      </div>

      <details class="archive">
        <summary><span>탈락·보관 모델</span><b>{archivedModels.length}명</b></summary>
        {archivedModels.map((model) => (
          <div class="archive-row">
            <img src={model.image} alt="" loading="lazy" />
            <div><small>{model.code} · ARCHIVED</small><h3>{model.name}</h3><p>{model.role}</p></div>
            <strong>사용자 검토에서 탈락 · 이후 Soul ID와 광고에 사용하지 않음</strong>
          </div>
        ))}
      </details>
    </section>

    <section class="library-section" id="videos">
      <div class="container">
        <header class="section-head light">
          <div><p class="eyebrow">Campaign library</p><h2>영상이 늘어나도 같은 구조로 관리</h2></div>
          <p>각 영상은 고유 ID, 모델, 훅, 상태, 콘티, 미리보기와 최종 파일을 가집니다. 새 항목을 추가하면 이 라이브러리에 자동으로 카드가 생성됩니다.</p>
        </header>

        <div class="video-grid">
          {campaignVideos.map((video) => (
            <article class="video-card">
              <div class="video-preview">
                {video.videoSrc ? <video controls preload="metadata" poster={video.poster}><source src={video.videoSrc} type="video/mp4" /></video> : <img src={video.poster} alt={`${video.title} 콘티 미리보기`} />}
                <span class="seq">#{String(video.sequence).padStart(2, '0')}</span>
                <span class:list={['video-status', video.status]}>{statusLabel[video.status]}</span>
              </div>
              <div class="video-body">
                <div class="video-id"><span>{video.id}</span><b>{video.duration} · {video.ratio}</b></div>
                <h3>{video.title}</h3><p>{video.subtitle}</p>
                <dl>
                  <div><dt>모델</dt><dd>{video.modelLabel}</dd></div>
                  <div><dt>목표</dt><dd>{video.objective}</dd></div>
                  <div><dt>훅</dt><dd>“{video.hook}”</dd></div>
                </dl>
                <a href={`#video-${String(video.sequence).padStart(3, '0')}`}>콘티 열기 →</a>
              </div>
            </article>
          ))}
        </div>

        <aside class="library-schema">
          <div><span>필수 등록값</span><strong>ID · 제목 · 상태 · 모델 · 훅 · 비율 · 길이</strong></div>
          <div><span>선택 등록값</span><strong>MP4 · 고화질 마스터 · 포스터 · 성과지표</strong></div>
          <div><span>지원 상태</span><strong>콘티 → 캐스팅 → 제작 → 검수 → 승인 → 게시</strong></div>
        </aside>
      </div>
    </section>

    <section class="concept-section container" id="video-001">
      <header class="concept-title">
        <div class="number">01</div>
        <div><p class="eyebrow">First production · {firstVideo.id}</p><h2>{firstVideo.title}</h2><p>{firstVideo.subtitle}</p></div>
        <span class="concept-status">{statusLabel[firstVideo.status]}</span>
      </header>

      <div class="brief-grid">
        <article><span>목표</span><p>{firstVideo.objective}</p></article>
        <article><span>핵심 상황</span><p>토너만으로는 아쉽고 크림은 무겁다고 느끼는 실제 루틴 순간</p></article>
        <article><span>키 메시지</span><strong>{firstVideo.keyMessage}</strong></article>
        <article><span>CTA</span><strong>{firstVideo.cta}</strong></article>
        {firstVideo.generation && <article><span>생성 경로</span><p>{firstVideo.generation}</p></article>}
        {firstVideo.creditSummary && <article><span>Higgsfield 크레딧</span><strong>{firstVideo.creditSummary}</strong></article>}
      </div>

      <div class="script-strip">
        <span>30초 나레이션</span>
        <div><p>“{firstVideo.narrationScript}”</p><small>{firstVideo.voiceDirection}</small></div>
      </div>

      <section class="sketches" aria-labelledby="sketch-title">
        <div class="subhead"><p class="eyebrow">GPT Images previsualization</p><h3 id="sketch-title">말하는 얼굴 대신 무언 행동으로 설계</h3><p>실제 제품 원본을 레퍼런스로 첨부해 ChatGPT Images 웹에서 다시 만든 30초 기획 스케치입니다. 인물의 입은 닫고, 정보는 모두 나레이션과 후반 자막으로 전달합니다.</p></div>
        <div class="sketch-grid">
          {storySketches.map((sketch) => (
            <figure><img src={sketch.src} alt={sketch.label + ' 스토리보드 스케치'} /><figcaption><b>{sketch.label}</b><span>{sketch.desc}</span></figcaption></figure>
          ))}
        </div>
      </section>

      <section class="timeline" aria-labelledby="timeline-title">
        <div class="subhead"><p class="eyebrow">Shot by shot</p><h3 id="timeline-title">30초 나레이션 콘티</h3></div>
        <div class="timeline-list">
          {firstVideo.storyboard.map((shot, index) => (
            <article>
              <div class="shot-index"><span>{shot.id}</span><b>{shot.time}</b><em>{shot.phase}</em></div>
              <div class="shot-main"><h4>{shot.visual}</h4><p class="dialogue">나레이션 · “{shot.narration}”</p>{shot.subtitle && <p class="subtitle">자막 · {shot.subtitle}</p>}</div>
              <div class="shot-tech"><span>{shot.method}</span><p>{shot.qa}</p></div>
              <i>{String(index + 1).padStart(2, '0')}</i>
            </article>
          ))}
        </div>
      </section>

      <div class="do-dont">
        <article><span>반드시 지킬 것</span><ul><li>모델은 입을 닫고 무언 행동만 연기</li><li>실제 한국인 여성 나레이션 우선</li><li>실제 제품 PNG로 라벨 인증</li><li>한국어 자막은 후반 합성</li></ul></article>
        <article class="dont"><span>사용하지 않을 것</span><ul><li>모델 립싱크와 카메라 대화</li><li>AI가 만든 펌핑·제형 증거</li><li>느린 소프트톤·AI 성우 억양</li><li>검은 자막 박스와 긴 엔드카드</li></ul></article>
      </div>
    </section>

    <section class="gates-section" id="gates">
      <div class="container">
        <header class="section-head"><div><p class="eyebrow">Failure-control gates</p><h2>한 번에 만들지 않는 제작 순서</h2></div><p>앞 단계가 통과되지 않으면 다음 단계 크레딧을 사용하지 않습니다.</p></header>
        <div class="gate-list">
          {productionGates.map(([number, title, body], index) => (
            <article class:list={{ done: index < 5, current: index === 5 }}><span>{number}</span><div><h3>{title}</h3><p>{body}</p></div><b>{index < 5 ? 'CHECKED' : index === 5 ? 'NOW' : 'NEXT'}</b></article>
          ))}
        </div>
      </div>
    </section>
    </details>
  </main>
  <SiteFooter />
</BaseLayout>

<script>
  const filterButtons = Array.from(document.querySelectorAll<HTMLButtonElement>('[data-video-filter]'));
  const videoCards = Array.from(document.querySelectorAll<HTMLElement>('[data-video-state]'));
  const emptyState = document.querySelector<HTMLElement>('[data-video-empty]');

  const applyVideoFilter = (filter: string) => {
    let visibleCount = 0;
    videoCards.forEach((card) => {
      const visible = filter === 'all' || card.dataset.videoState === filter;
      card.hidden = !visible;
      if (visible) visibleCount += 1;
    });
    filterButtons.forEach((button) => {
      const active = button.dataset.videoFilter === filter;
      button.classList.toggle('active', active);
      button.setAttribute('aria-pressed', String(active));
    });
    if (emptyState) emptyState.hidden = visibleCount !== 0;
  };

  filterButtons.forEach((button) => button.addEventListener('click', () => applyVideoFilter(button.dataset.videoFilter || 'all')));

  const productionDetails = document.querySelector<HTMLDetailsElement>('#production-details');
  document.querySelectorAll<HTMLElement>('[data-open-production-details]').forEach((link) => {
    link.addEventListener('click', () => {
      if (productionDetails) productionDetails.open = true;
    });
  });
  productionDetails?.addEventListener('toggle', () => {
    const label = productionDetails.querySelector<HTMLElement>('summary strong');
    if (label) label.textContent = productionDetails.open ? '제작 상세 접기' : '제작 상세 펼치기';
  });
</script>

<style>
  .studio-page { --aqua:#bfe8e9; --aqua-deep:#285e62; --paper:#f4f0e7; --night:#101716; color:var(--ink); overflow:hidden; }
  .studio-page :is(h1,h2,h3,h4,p,dl,blockquote,figure) { margin-top:0; }
  :global(.studio-page img), :global(.studio-page video) { max-width:100%; }
  :global(.attempt-media img), :global(.attempt-media video), :global(.model-visual img), :global(.video-preview img), :global(.video-preview video), :global(.sketch-grid img) { display:block; width:100%; height:100%; object-fit:cover; }
  .studio-page a { color:inherit; text-decoration:none; }
  .eyebrow { margin:0 0 10px; text-transform:uppercase; letter-spacing:.17em; font-size:12px; font-weight:900; color:var(--aqua-deep); }

  .hero { display:grid; grid-template-columns:minmax(0,1.2fr) minmax(340px,.8fr); gap:clamp(28px,6vw,90px); align-items:center; padding-block:clamp(44px,8vw,96px) clamp(32px,6vw,72px); }
  .hero h1 { margin:0; max-width:16ch; font-size:clamp(40px,6.3vw,82px); line-height:.96; letter-spacing:-.055em; font-weight:900; }
  .hero h1 em { color:var(--aqua-deep); font-style:normal; }
  .hero-copy>p:not(.eyebrow) { max-width:60ch; margin:25px 0; color:var(--muted); font-size:clamp(16px,1.5vw,19px); line-height:1.75; }
  .hero-actions { display:flex; flex-wrap:wrap; gap:10px; }
  .hero-actions a { padding:13px 18px; border-radius:999px; background:var(--night); color:white; font-weight:800; }
  .hero-actions .ghost { border:1px solid var(--line-strong); background:transparent; color:var(--ink); }
  .hero-board { position:relative; transform:rotate(1.5deg); border:1px solid #27302e; border-radius:6px; background:var(--night); color:#f4f0e7; box-shadow:18px 22px 0 #cce8e7; }
  .board-top,.board-main,.hero-board dl { padding:18px 22px; }
  .board-top { display:flex; justify-content:space-between; border-bottom:1px dashed #56605e; font:800 12px/1 monospace; letter-spacing:.08em; }
  .board-main { min-height:190px; display:flex; flex-direction:column; justify-content:center; }
  .board-main>strong { color:#93d5d5; font:700 13px monospace; }.board-main h2{margin:9px 0 20px;font-size:clamp(34px,4vw,54px);letter-spacing:-.045em}.board-main p{margin:0;color:#c7cecc}.board-main p b{color:white}
  .hero-board dl { display:grid;grid-template-columns:repeat(3,1fr);gap:10px;border-top:1px dashed #56605e;margin:0 }.hero-board dt{color:#96a09d;font-size:11px}.hero-board dd{margin:5px 0 0;font:900 19px monospace;color:white}

  .section-nav { position:sticky; top:8px; z-index:10; display:flex; justify-content:center; gap:6px; width:max-content; max-width:calc(100vw - 24px); padding:7px; border:1px solid rgba(16,23,22,.16); border-radius:999px; background:rgba(244,240,231,.92); backdrop-filter:blur(14px); box-shadow:0 10px 30px rgba(16,23,22,.08); }
  .section-nav a { padding:9px 14px; border-radius:999px; font-size:13px; font-weight:800; white-space:nowrap; }.section-nav a:hover{background:var(--night);color:white}
  .section-head { display:flex; justify-content:space-between; gap:30px; align-items:end; margin-bottom:28px; }.section-head h2{margin:0;font-size:clamp(32px,4vw,54px);letter-spacing:-.045em;word-break:keep-all}.section-head>p{max-width:54ch;margin:0;color:var(--muted);line-height:1.65}.section-head.light{color:white}.section-head.light .eyebrow{color:#91d7d6}.section-head.light>p{color:#b7c0bd}

  .attempt-section{padding-block:clamp(70px,9vw,115px)}
  .attempt-summary{display:grid;grid-template-columns:repeat(4,1fr);border:1px solid var(--line);border-radius:16px;overflow:hidden;background:white;margin-bottom:22px}.attempt-summary div{padding:17px 20px;border-right:1px solid var(--line)}.attempt-summary div:last-child{border:0}.attempt-summary span{display:block;color:var(--muted);font-size:11px;font-weight:800}.attempt-summary b{display:inline-block;margin-top:5px;font:900 25px monospace}.attempt-summary small{margin-left:5px;color:var(--muted);font:800 10px monospace}
  .attempt-lane{margin-top:24px;padding:22px;border:1px solid var(--line);border-radius:24px;background:#edf4f2;overflow:hidden}.video-lane{scroll-margin-top:80px;background:var(--night);border-color:var(--night);color:white}.lane-head{display:flex;align-items:end;justify-content:space-between;gap:24px;margin-bottom:18px}.lane-head h3{margin:2px 0 0;font-size:clamp(26px,3vw,39px);letter-spacing:-.04em}.lane-head>p{max-width:50ch;margin:0;color:var(--muted);font-size:13px;line-height:1.55}.video-lane .lane-head>p{color:#aeb9b6}.lane-kicker{margin:0;color:var(--aqua-deep);font:900 10px monospace;letter-spacing:.12em}.video-lane .lane-kicker{color:#8bd2cf}.attempt-rail{display:grid;grid-auto-flow:column;grid-auto-columns:minmax(286px,330px);gap:14px;overflow-x:auto;overscroll-behavior-inline:contain;scroll-snap-type:inline mandatory;padding:2px 2px 12px;scrollbar-color:#8aa09b transparent}.video-attempt-rail{grid-auto-columns:minmax(310px,350px)}.attempt-card{min-width:0;overflow:hidden;border:1px solid var(--line);border-radius:18px;background:white;color:var(--ink);box-shadow:var(--shadow-sm);scroll-snap-align:start}.attempt-card[hidden]{display:none!important}.attempt-card.rejected{border-color:#e5c2ba;background:#fffaf8}.attempt-card.pending{border-color:#d6be79}.attempt-card.reference{border-color:#8fc4ba}.attempt-media{position:relative;aspect-ratio:4/5;overflow:hidden;background:#dfe8e6}.video-attempt .attempt-media{aspect-ratio:9/11;background:#111}.video-attempt .attempt-media video{object-fit:contain;background:#111}.attempt-media img,.attempt-media video{display:block;width:100%;height:100%;object-fit:cover}.attempt-scene,.attempt-status{position:absolute;top:11px;padding:6px 8px;border-radius:999px;font:900 9px monospace}.attempt-scene{left:11px;background:var(--night);color:white}.attempt-status{right:11px;background:#e9f6f2;color:#23604f}.rejected .attempt-status{background:#ffe5df;color:#983c31}.pending .attempt-status{background:#fff1bd;color:#765a00}.attempt-copy{padding:16px}.attempt-id{display:flex;justify-content:space-between;gap:10px;color:var(--muted);font:800 9px monospace}.attempt-copy h3{margin:14px 0 6px;font-size:20px;letter-spacing:-.03em}.attempt-copy>p{min-height:58px;color:var(--muted);font-size:12px;line-height:1.55;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}.attempt-copy dl{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin:14px 0}.attempt-copy dl div{padding:8px;border-radius:8px;background:#f1f4f2}.attempt-copy dt{font-size:9px;color:var(--muted)}.attempt-copy dd{margin:3px 0 0;font-size:10px;font-weight:800;line-height:1.4}.attempt-copy ul{margin:9px 0 0;padding-left:17px;color:#873f36;font-size:11px;line-height:1.55}.attempt-issues{border-top:1px solid var(--line);padding-top:10px}.attempt-issues summary{display:flex;align-items:center;justify-content:space-between;cursor:pointer;color:#7d4a42;font-size:11px;font-weight:900;list-style:none}.attempt-issues summary::-webkit-details-marker{display:none}.attempt-issues summary b{display:grid;place-items:center;width:20px;height:20px;border-radius:50%;background:#f4dfda;font:900 10px monospace}.video-lane-head{align-items:center}.video-filters{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:7px}.video-filters button{border:1px solid #394440;border-radius:999px;background:#1c2624;color:#bec9c6;padding:8px 11px;font-weight:800;cursor:pointer}.video-filters button b{margin-left:5px;color:white;font:900 11px monospace}.video-filters button.active{background:white;color:var(--night);border-color:white}.video-filters button.active b{color:var(--aqua-deep)}.video-outcome-strip{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:16px}.video-outcome-strip>div{display:grid;grid-template-columns:auto 1fr;align-items:center;gap:1px 10px;padding:11px 13px;border:1px solid #35423f;border-radius:12px;background:#18211f}.video-outcome-strip span{font:900 9px monospace;letter-spacing:.1em}.video-outcome-strip b{grid-row:1/3;grid-column:2;justify-self:end;font:900 25px monospace}.video-outcome-strip small{color:#96a39f;font-size:10px}.video-outcome-strip .success span,.video-outcome-strip .success b{color:#79dfac}.video-outcome-strip .review span,.video-outcome-strip .review b{color:#e9c466}.video-outcome-strip .failed span,.video-outcome-strip .failed b{color:#ff9484}.filter-empty{padding:30px;border:1px dashed #51605c;border-radius:14px;text-align:center;color:#b8c2bf;background:#17201e}.filter-empty[hidden]{display:none}

  .dashboard-details{border-top:1px solid var(--line);background:#eaf1ef}.dashboard-details>summary{display:flex;align-items:center;justify-content:space-between;gap:20px;padding-block:24px;cursor:pointer;list-style:none}.dashboard-details>summary::-webkit-details-marker{display:none}.dashboard-details>summary span{display:grid;gap:4px}.dashboard-details>summary small{color:var(--aqua-deep);font:900 10px monospace;letter-spacing:.12em}.dashboard-details>summary b{font-size:clamp(18px,2.4vw,29px);letter-spacing:-.03em}.dashboard-details>summary strong{padding:10px 14px;border-radius:999px;background:var(--night);color:white;font-size:12px;white-space:nowrap}.dashboard-details[open]>summary{border-bottom:1px solid #c9d5d2}.dashboard-details[open]>summary strong{background:white;color:var(--night);border:1px solid var(--line)}

  .models-section { padding-block:clamp(70px,10vw,130px); }
  .model-grid { display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:18px; }
  .model-card { min-width:0; border:1px solid var(--line); border-radius:24px; overflow:hidden; background:white; box-shadow:var(--shadow-sm); }
  .model-visual { position:relative; aspect-ratio:4/5; overflow:hidden; background:linear-gradient(#e8edf0,#f4f1eb); }.model-visual img{width:100%;height:100%;object-fit:cover}.model-code,.model-visual .status{position:absolute;top:16px}.model-code{left:16px;display:grid;place-items:center;width:48px;height:48px;border-radius:50%;background:var(--night);color:white;font:900 25px monospace}.model-visual .status{right:16px;padding:7px 10px;border-radius:999px;background:#e5fff1;color:#13693f;font:900 11px monospace}
  .model-copy { padding:22px; }.model-copy .role{margin:0 0 5px;color:var(--aqua-deep);font-weight:800;font-size:13px}.model-copy h3{margin:0;font-size:31px;letter-spacing:-.035em}.measure{display:flex;gap:8px;align-items:center;margin:13px 0}.measure>*{padding:5px 9px;border-radius:999px;background:var(--paper);font-size:12px}.styling{min-height:46px;color:var(--muted);line-height:1.55}.model-copy dl{display:grid;gap:8px;margin:18px 0}.model-copy dl div{display:grid;grid-template-columns:72px 1fr;gap:8px;padding-top:8px;border-top:1px solid var(--line)}.model-copy dt{font-size:12px;color:var(--muted)}.model-copy dd{margin:0;font-size:13px;font-weight:700;line-height:1.45}.model-copy blockquote{margin:18px 0;padding:15px;border-left:3px solid var(--aqua-deep);background:#eff8f7;font-weight:800;line-height:1.55}
  .signal-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.signal-grid>div{padding:13px;border-radius:13px;background:#f0f7f2}.signal-grid .risk{background:#fbf2ef}.signal-grid b{display:block;margin-bottom:7px;font-size:12px}.signal-grid span{display:block;color:var(--muted);font-size:12px;line-height:1.45;margin-top:3px}
  .archive{margin-top:18px;border:1px solid var(--line);border-radius:16px;background:var(--surface);overflow:hidden}.archive summary{display:flex;justify-content:space-between;padding:17px 20px;cursor:pointer;font-weight:800}.archive-row{display:grid;grid-template-columns:80px 1fr minmax(220px,.8fr);gap:18px;align-items:center;padding:18px;border-top:1px solid var(--line)}.archive-row img{width:80px;height:80px;object-fit:cover;filter:grayscale(1)}.archive-row small{color:#9a3c34;font-weight:900}.archive-row h3{margin:3px 0}.archive-row p,.archive-row strong{margin:0;color:var(--muted);font-size:13px;line-height:1.5}

  .library-section { padding-block:clamp(70px,10vw,120px); background:var(--night); }
  .video-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px}.video-card{display:grid;grid-template-columns:minmax(240px,.8fr) minmax(0,1.2fr);overflow:hidden;border:1px solid #35403d;border-radius:20px;background:#18201f;color:white}.video-preview{position:relative;min-height:440px;background:#c9d9d7}.video-preview img,.video-preview video{width:100%;height:100%;object-fit:cover}.seq,.video-status{position:absolute;top:15px;padding:8px 10px;border-radius:999px;font:900 11px monospace}.seq{left:15px;background:var(--night);color:white}.video-status{right:15px;background:#c7eee9;color:#164e50}.video-body{padding:25px;display:flex;flex-direction:column}.video-id{display:flex;justify-content:space-between;gap:12px;color:#9eaaa7;font:700 11px monospace}.video-body h3{margin:28px 0 4px;font-size:36px;letter-spacing:-.04em}.video-body>p{color:#b4bfbc}.video-body dl{display:grid;gap:11px;margin:24px 0}.video-body dl div{padding-top:10px;border-top:1px solid #36403e}.video-body dt{color:#82908c;font-size:11px}.video-body dd{margin:5px 0 0;font-size:13px;line-height:1.5}.video-body>a{margin-top:auto;align-self:flex-start;padding:11px 14px;border-radius:999px;background:white;color:var(--night);font-weight:900;font-size:13px}
  .library-schema{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;margin-top:22px;background:#394441;border:1px solid #394441}.library-schema div{padding:18px;background:#161e1d;color:white}.library-schema span{display:block;margin-bottom:6px;color:#91a09c;font-size:11px}.library-schema strong{font-size:13px;line-height:1.5}

  .concept-section { padding-block:clamp(75px,10vw,140px); }.concept-title{display:grid;grid-template-columns:auto 1fr auto;gap:22px;align-items:center;padding-bottom:24px;border-bottom:3px solid var(--night)}.concept-title .number{font:900 clamp(55px,8vw,105px)/.8 monospace;color:#b9dedd}.concept-title h2{margin:0;font-size:clamp(44px,6vw,76px);letter-spacing:-.055em}.concept-title>div>p:last-child{margin:10px 0 0;color:var(--muted)}.concept-status{padding:9px 13px;border-radius:999px;background:#f3d9a6;font-size:12px;font-weight:900}
  .brief-grid{display:grid;grid-template-columns:2fr 2fr 1fr 1fr;border-bottom:1px solid var(--line)}.brief-grid article{min-width:0;padding:21px;border-right:1px solid var(--line)}.brief-grid article:first-child{padding-left:0}.brief-grid article:last-child{border:0}.brief-grid span{display:block;margin-bottom:8px;color:var(--muted);font-size:11px;font-weight:900}.brief-grid p,.brief-grid strong{margin:0;line-height:1.55}.script-strip{display:grid;grid-template-columns:150px 1fr;gap:20px;margin:32px 0;padding:24px;border-radius:18px;background:var(--aqua)}.script-strip span{font-size:12px;font-weight:900}.script-strip p{margin:0;font-size:clamp(17px,2vw,23px);line-height:1.6;font-weight:800;letter-spacing:-.015em}.script-strip small{display:block;margin-top:12px;color:#356568;font-size:12px;line-height:1.55;font-weight:800}
  .subhead{max-width:760px;margin:0 0 24px}.subhead h3{margin:0 0 10px;font-size:clamp(30px,4vw,48px);letter-spacing:-.04em}.subhead>p:last-child{color:var(--muted);line-height:1.65}
  .sketches{padding-block:35px 65px}.sketch-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px}.sketch-grid figure{overflow:hidden;border:1px solid var(--line);border-radius:18px;background:white}.sketch-grid img{display:block;width:100%;aspect-ratio:4/5;object-fit:cover;background:#eee}.sketch-grid figcaption{display:grid;gap:6px;padding:16px}.sketch-grid figcaption span{color:var(--muted);font-size:13px;line-height:1.5}
  .timeline{padding-top:35px}.timeline-list{border-top:3px solid var(--night)}.timeline-list article{position:relative;display:grid;grid-template-columns:150px minmax(0,1.5fr) minmax(220px,.7fr);gap:22px;padding:24px 44px 24px 0;border-bottom:1px solid var(--line)}.shot-index{display:flex;flex-direction:column;gap:5px}.shot-index span{font:900 18px monospace}.shot-index b{font-size:14px}.shot-index em{align-self:flex-start;padding:4px 7px;border-radius:5px;background:var(--night);color:white;font:800 10px monospace;font-style:normal}.shot-main h4{margin:0 0 12px;font-size:17px;line-height:1.55}.shot-main p{margin:5px 0;color:var(--muted);font-size:13px}.shot-main .subtitle{color:var(--aqua-deep);font-weight:800}.shot-tech{padding-left:20px;border-left:1px solid var(--line)}.shot-tech span{font-size:12px;font-weight:900}.shot-tech p{margin:8px 0 0;color:var(--muted);font-size:12px;line-height:1.5}.timeline-list i{position:absolute;right:0;top:24px;color:#c7d0cd;font:900 21px monospace;font-style:normal}
  .do-dont{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:38px}.do-dont article{padding:22px;border-radius:18px;background:#e9f3ec}.do-dont article.dont{background:#f8e9e5}.do-dont span{font-weight:900}.do-dont ul{margin:14px 0 0;padding-left:18px;color:var(--muted);line-height:1.8}
  .gates-section{padding-block:clamp(70px,10vw,120px);background:#e3edeb}.gate-list{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.gate-list article{display:grid;grid-template-columns:auto 1fr auto;gap:13px;align-items:start;padding:20px;border:1px solid #bfceca;border-radius:15px;background:#f4f7f5}.gate-list article.done{border-color:#85b9ad;background:#edf8f3}.gate-list article.current{background:var(--night);color:white;border-color:var(--night)}.gate-list>article>span{font:900 17px monospace;color:var(--aqua-deep)}.gate-list .current>span{color:#91dbd8}.gate-list h3{margin:0 0 7px;font-size:17px}.gate-list p{margin:0;color:var(--muted);line-height:1.55}.gate-list b{font:900 10px monospace;letter-spacing:.08em}.gate-list .done>b{color:#26715e}.gate-list .current p{color:#cdd5d2}

  @media(max-width:1050px){.hero{grid-template-columns:1fr}.hero-board{max-width:620px}.model-grid{grid-template-columns:1fr 1fr}.model-card:last-child{grid-column:1/-1;display:grid;grid-template-columns:1fr 1fr}.video-grid{grid-template-columns:1fr}.sketch-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.gate-list{grid-template-columns:1fr 1fr}}
  @media(max-width:720px){.section-nav{justify-content:flex-start;overflow-x:auto}.section-head{display:block}.section-head>p{margin-top:13px}.attempt-summary{grid-template-columns:1fr 1fr}.attempt-summary div:nth-child(2){border-right:0}.attempt-summary div:nth-child(-n+2){border-bottom:1px solid var(--line)}.attempt-lane{padding:16px;margin-top:16px;border-radius:18px}.lane-head{display:block}.lane-head>p{margin-top:9px}.video-lane-head{display:grid;gap:14px}.video-filters{justify-content:flex-start;overflow-x:auto;flex-wrap:nowrap;padding-bottom:3px}.video-filters button{white-space:nowrap}.video-outcome-strip{grid-template-columns:1fr 1fr 1fr}.video-outcome-strip>div{display:flex;flex-direction:column;align-items:flex-start;gap:3px;padding:10px}.video-outcome-strip b{order:-1;font-size:22px}.video-outcome-strip small{line-height:1.35}.attempt-rail{grid-auto-columns:minmax(270px,84vw)}.video-attempt-rail{grid-auto-columns:minmax(282px,86vw)}.attempt-copy>p{min-height:0}.model-grid{grid-template-columns:1fr}.model-card:last-child{display:block;grid-column:auto}.archive-row{grid-template-columns:58px 1fr}.archive-row img{width:58px;height:58px}.archive-row>strong{grid-column:1/-1}.video-card{grid-template-columns:1fr}.video-preview{min-height:390px}.library-schema{grid-template-columns:1fr}.concept-title{grid-template-columns:auto 1fr}.concept-status{grid-column:1/-1;justify-self:start}.brief-grid{grid-template-columns:1fr 1fr}.brief-grid article{padding:16px 12px;border-bottom:1px solid var(--line)}.script-strip{grid-template-columns:1fr}.sketch-grid{grid-template-columns:1fr}.timeline-list article{grid-template-columns:1fr;padding-right:30px}.shot-tech{padding:12px 0 0;border-left:0;border-top:1px solid var(--line)}.do-dont{grid-template-columns:1fr}.gate-list{grid-template-columns:1fr}}
  @media(max-width:430px){.hero h1{font-size:42px}.hero-board{transform:none;box-shadow:8px 10px 0 #cce8e7}.hero-board dl{grid-template-columns:1fr 1fr}.hero-board dl div:last-child{grid-column:1/-1}.model-copy{padding:18px}.signal-grid{grid-template-columns:1fr}.video-body h3{font-size:31px}.brief-grid{grid-template-columns:1fr}.brief-grid article{padding-inline:0;border-right:0}.concept-title .number{font-size:48px}.concept-title h2{font-size:40px}}
</style>
